const bar=document.getElementById('progress');const nav=[...document.querySelectorAll('nav a')];const sections=[...document.querySelectorAll('main section[id]')];function update(){const d=document.documentElement;const max=d.scrollHeight-d.clientHeight;bar.style.width=(max?d.scrollTop/max*100:0)+'%'}addEventListener('scroll',update,{passive:true});update();const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){nav.forEach(a=>a.classList.remove('active'));const a=nav.find(x=>x.getAttribute('href')==='#'+e.target.id);if(a)a.classList.add('active')}})},{rootMargin:'-22% 0px -68% 0px'});sections.forEach(s=>io.observe(s));